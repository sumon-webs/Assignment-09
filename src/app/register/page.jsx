import RegisterForm from '@/components/RegisterForm';

export const metadata = {
    title: "DocAppoint || Register",
};
const page = () => {
    return (
        <div>
            <RegisterForm />
        </div>
    );
};

export default page;
import LogInForm from '@/components/LogInForm';
export const metadata = {
    title: "DocAppoint || Log in",
};
const page = () => {
    return (
        <div>
            <LogInForm/>
        </div>
    );
};

export default page;